apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-messenger
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Solierrr/infra-platform.git
    targetRevision: main
    path: k8s/apps/api-messenger
  destination:
    server: https://kubernetes.default.svc
    namespace: solierrr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
