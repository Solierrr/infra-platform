apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-auth
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Solierrr/infra-platform.git
    targetRevision: main
    path: k8s/apps/api-auth
  destination:
    server: https://kubernetes.default.svc
    namespace: solierrr
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
